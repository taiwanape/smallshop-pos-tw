# 0908 影片分析與整合說明

來源：使用者提供的 `C:\Users\taiwa\Desktop\0908.mp4`，片長約 5 分 3 秒。分析方式是抽取字幕與關鍵畫面逐段核對；未取得原網站網址或登入原產品。

## 影片可核實內容

- 00:25：產品是一個網站。
- 00:31–00:32：用途是班級管理，對象包含老師。
- 01:47–01:55：從純積分改為養寵物，把積分兌換食物，再餵養寵物。
- 02:03–02:05：受訪者表示卡通圖案由 AI 輔助生成。
- 00:35、03:06、03:10：螢幕顯示多個動物卡片與進度條；細字不足以辨識全部按鈕和規則。

影片已移除網站介紹，因此不宣稱辨識了原站名稱、完整功能或驗證影片中的營收。02:51 起另一款 App 的計時器畫面，也不歸類為班級工具。

## 本次自行實作

「班級小夥伴」重現可核實的積分→食物→餵養主流程。5 積分換 1 食物；1 食物增加 5 成長；每 20 成長提升一級。這些數值、等級規則、預設評分、班級管理、分組、隨機點名、排行、撤銷、備份及 CSV 都是本次自行設計的延伸，不宣稱影片已演示。寵物使用裝置原生 emoji，未复制原網站圖案。

班級資料儲存在此瀏覽器 IndexedDB `daily-tools-classroom-v1`。批次操作採單一交易，資源不足時全部失敗；事件重試不重複計分；跨分頁寫入序列化並通知更新。撤銷需有足夠剩餘資源。匯入以新 ID 加入新班級，保留原資料。沒有雲端同步、家長端、會員或訂閱收款。

## 同一個網站

- `/`：日常工具所，共用入口。
- `/classroom`：班級小夥伴。
- `/pos`：原本完整小店快收操作介面。原 `app/page.tsx` 移至 `components/pos-app.tsx`，保留原 IndexedDB、備份、列印、交易邏輯與各項限制。
- `/learn`：共用導覽內的 LexiHarbor，完整靜態匯出位於 `/lexiharbor/`，隔離兩套 UI 的 CSS 與 React 執行環境。保留所有字卡、文章、字典、語音、插圖和授權資源。

原 POS 靜態產品介紹仍由 `web-preview/main.tsx` 和 `components/public-showcase.tsx` 建置，不因首頁改為工具入口而載入錯誤元件。原 GitHub Pages 發布未變更。

同一來源網域的 POS 本機資料可繼續讀取。LexiHarbor 舊 GitHub Pages 與整合站屬不同來源，需在原站設定匯出，再到整合站匯入。網站本身沒有存取或搬移使用者瀏覽器內的私人資料。

## 更新與驗證

LexiHarbor 的可發布版本來自同專案 `../lexiharbor/dist`，由該專案 `build:web` 產生。更新英文工具後，執行本專案 `scripts/sync-lexiharbor.mjs` 更新資源，再重新建置整合站。原始碼仍維護於相鄰 `lexiharbor` 專案；不把兩套不同版本的 React 強行混合。

本次驗證包括 TypeScript、既有 POS 測試、班級領域及資料交易測試、整合資源檢查、正式建置與 HTTP 路由檢查。未執行瀏覽器互動測試，未宣稱完成商用驗收。
